<Window x:Class="ScreenshotProgramm.Views.TextInputWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Text" Height="180" Width="420" WindowStartupLocation="CenterOwner" ResizeMode="NoResize">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Text:" Margin="0,0,0,6"/>
        <TextBox x:Name="InputText" Grid.Row="1" AcceptsReturn="True" TextWrapping="Wrap"/>
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,8,0,0">
            <Button Width="90" Content="OK" Click="OkClicked"/>
            <Button Width="90" Content="Cancel" Click="CancelClicked"/>
        </StackPanel>
    </Grid>
</Window>
